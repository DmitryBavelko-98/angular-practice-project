import { GmailValidatorDirective } from './gmail-validator.directive';

describe('GmailValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new GmailValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
